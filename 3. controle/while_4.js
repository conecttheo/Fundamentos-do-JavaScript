let controle = 1; // variavel que ajuda a controlar o laço

while(controle <= 10) {     //expressão que precisa mudar 
    console.log(controle);  //durante a execução do codigo
    controle++; //logica que modificasse o valor da variavel
}

console.log("fim");

// declaração; expressão; incremento = qtd determinada